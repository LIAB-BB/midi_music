# 候选能力矩阵与证据边界

更新日期：2026-08-24。固定状态枚举：实现 `{是,否,Legacy}`；自动化 `{是,部分,否}`；真机 `{已记录,待记录,不适用}`；试用 `{是,待判定,否}`。说明统一放最后一列，避免把“有代码”写成“已试用”。

| 能力 | 实现 | 自动化 | 真机 | 试用 | 事实与边界 |
| --- | --- | --- | --- | --- | --- |
| K.478 首页预载、播放、暂停、seek | 是 | 是 | 待记录 | 待判定 | 候选 host 载入固定 MIDI 与离线弦乐；真机听音和完整 UI 验收未完成。 |
| iPhone-only / iOS 13.0 | 是 | 部分 | 待记录 | 待判定 | 当前工作区无签名 bundle audit 已通过（UIDeviceFamily=1）；CI、签名和真机仍待。 |
| CoreMIDI 输入（首轮直连 USB 验收） | 是 | 部分 | 待记录 | 待判定 | 自动化覆盖 Dart 输入/会话契约；原生 CoreMIDI 与真实设备尚未验证。不按设备名过滤 USB，不承诺虚拟、网络、蓝牙或 Hub 通过。 |
| 固定轨 4/5 跟随、轨 1/2/3 弦乐 | 是 | 是 | 待记录 | 待判定 | 电子琴自发声；没有通用选轨。 |
| 自动 Wait Mode | 否 | 是 | 待记录 | 否 | K.478 当前没有人工批准的全体等待点；钢琴单独休止时弦乐持续。底层仅保留已批准边界的通用机制，未在当前候选启用。 |
| K.478 PDF 页面阅读 | 是 | 是 | 待记录 | 待判定 | 21 张已复现核验 PNG；手动入口，无自动翻页或同步。 |
| Violin/Cello SF2 伴奏 | 是 | 是 | 待记录 | 待判定 | 中提琴声部暂用小提琴音色；仍缺官方输入比对、签名 IPA 与 iPhone 听音。 |
| Bundle 二进制审计 | 是 | 部分 | 待记录 | 待判定 | 审计候选包精确资源、`cupertino_icons` 图标字体、外层 host assets 和未知 package assets；CI、签名 Archive、Privacy Report 和真机仍待。 |
| MIDI 钢琴卷帘 | Legacy | 是 | 不适用 | 否 | 仅根开发 host 保留，不在候选演奏台。 |
| 通用 MIDI/MusicXML/PDF 导入 | Legacy | 是 | 待记录 | 否 | 根 host 保留，候选未接入。 |
| PDF OMR、麦克风跟随 | Legacy | 部分 | 待记录 | 否 | 不是候选二进制或测试承诺。 |
| Android MIDI | 否 | 否 | 不适用 | 否 | 当前候选只做 iPhone。 |

对外文案只能使用“试用=是”的能力。当前没有任何项目满足该列，因为外部证据尚未闭环。
