# 能力矩阵与证据边界

> 更新日期：2026-08-20。此表用来防止把“有代码”“有测试”“已真机验证”和“可对外发布”混为一谈。

## 状态定义

- **已实现**：存在受主路径调用的代码。
- **自动化覆盖**：存在针对该能力的测试；不等于真实设备成功。
- **真机证据**：有带 commit、设备、日期和结论的验收记录。
- **可试用**：满足当前范围、资产和发布门槛；不是仅靠代码推断。

| 能力 | 已实现 | 自动化覆盖 | 真机证据 | 可试用 | 事实与边界 |
| --- | --- | --- | --- | --- | --- |
| 内置 K.478 载入、播放、暂停、seek、弦乐开关 | 是 | 部分 | 待记录 | 待判定 | 首页直接载入内置 K.478；现有控制器测试覆盖分轨过滤与基础生命周期，真实音色及完整 UI 操作另计。 |
| 通用 MIDI / MusicXML / PDF 导入 | 保留旧实现 | 是 | 待记录 | 否 | 代码和测试保留在未接入旧页面；不是本轮导航或 TestFlight 能力。 |
| iOS CoreMIDI 设备发现与 Note On 输入（USB 验收） | 是 | Dart 桥接/会话层 | 待记录 | 待判定 | `CoreMidiInputPlugin` 是原生实现；每个端点单独维护 MIDI 字节流解析状态。模拟器不能替代外接电子琴验证；本轮只承诺以 USB 设备完成真机验收，不承诺虚拟、网络或蓝牙端点。 |
| USB MIDI 速度跟随 | 是 | 是 | 待记录 | 待判定 | 当前是预期音符匹配和速度平滑，不是错音、跳段或复调鲁棒跟随。 |
| 固定钢琴轨 4/5 不进入 App 音频 | 是 | 是 | 待记录 | 待判定 | K.478 候选不提供通用选轨；电子琴自行发声，播放器只调度轨道 1/2/3。 |
| K.478 PDF 分谱阅读 | 是 | Widget 入口覆盖 | 待记录 | 候选 | 离线页面图（由同源 PDF 渲染）；不随播放自动翻页。 |
| MIDI 钢琴卷帘 | 根开发 host 保留 | 根 host 测试 | 不适用 | 否 | 未删除既有实现，但首个独立候选演奏台不包含卷帘；未来版本可单独恢复。 |
| MusicXML 基础解析 | 保留旧实现 | 是 | 待记录 | 否 | 解析为播放数据；没有原生 MusicXML 五线谱渲染承诺。 |
| PDF OMR 导入 | 保留协议/骨架 | HTTP 契约 | 否 | 否 | 未配置受控服务时 App 会拒绝 PDF；服务骨架不等于可部署产品，也未接入本轮首页。 |
| 麦克风独奏跟随 | 保留旧实现 | 部分 | 否 | 否 | 当前产品 UI 使用 USB MIDI，不采集麦克风；旧实现和测试保留以待未来独立验证。 |
| 旧能力的 iOS 原生依赖与权限声明 | 保留于根开发 host | 否 | 否 | 否 | 根开发 host 仍注册文件选择、音频采集和权限插件并保留相关用途说明；这些代码未删除。独立候选 host `apps/testflight_ios` 只注册 `core_midi_input` 与 `flutter_midi_pro`，无麦克风/本地网络用途声明；最终仍需检查签名 Archive Privacy Report。 |
| Android MIDI 输入 | 否 | 否 | 否 | 否 | 当前产品仅 iOS；Android 目录不能视为该功能存在。 |
| App 伴奏 SoundFont | 候选 host 已实现 | 静态资产与双 `sfId` 路由/清理 | 否 | 待判定 | 候选 host 随包加载 Violin 40 与 Cello 42 两个单预设 PCM SF2；结构、哈希、许可留档及 macOS Apple sampler 离线渲染已通过。根开发 host 的 TimGM6mb 下载路径不进入候选。仍缺官方输入 SF3 全量比对、签名 IPA 检查与 iPhone 真机发声。 |
| 高质量 stem、时间拉伸、动态层 | 否 | 否 | 否 | 否 | 属于独立音频研发计划，不能计入当前 beta。 |
| 曲库商业分发 | 仅 K.478 有初步证据 | 资产解析覆盖 | 待法务台账 | 否 | 其他内置 MIDI 的来源/许可尚不充分。 |

## 使用规则

1. 对外文案只能使用“可试用”为“是”的能力。
2. 新功能进入范围前，必须先写入本表，再补实现、自动化测试和真机证据。
3. 真机记录格式见 [`../release_checklist.md`](../release_checklist.md)；资产记录见 [`../evidence/asset_manifest.md`](../evidence/asset_manifest.md)。
