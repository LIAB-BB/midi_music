# 上线前验收清单

本文用于发布或交付 iOS 试用版前的轻量人工验收。自动化测试负责基础契约，本文重点覆盖当前首页可到达的交互 MusicXML 谱面、MIDI-only 伴奏、播放控制和设置。

## 1. 文档定位

适用于每次准备发布或交付试用版前的最后一轮检查。

本文不替代以下自动化质量门禁：

```bash
flutter analyze
flutter test
```

## 2. 验收前准备

### 2.1 代码与依赖

- [ ] 使用干净工作区，或确认待验收改动已经提交 / 明确记录
- [ ] 已运行 `flutter pub get`
- [ ] 已运行 `flutter analyze`
- [ ] 已运行 `flutter test`
- [ ] iOS 验收前已运行 `cd ios && pod install`

### 2.2 设备与测试素材

- [ ] 准备一份可导入的有效 MIDI 文件
- [ ] 准备一份可离线导入的标准 MusicXML，包含首、中、末小节
- [ ] 准备一份无效或异常 MIDI 文件，用于导入失败路径
- [ ] 如需验证首次启动 SoundFont 下载，先清理应用数据或使用首次安装状态

## 3. 验收记录模板

| 项目 | 记录 |
|---|---|
| 验收日期 |  |
| 验收人 |  |
| Git commit |  |
| Flutter 版本 |  |
| 设备 / 系统版本 |  |
| 结论 | 通过 / 有阻塞 / 暂缓 |

## 4. 场景总览

建议按下表顺序执行。S1 准备音色，S2-S3 验证首页导入后的 MIDI-only 伴奏与固定控制栏，S4 覆盖设置，S5 覆盖交互 MusicXML 谱面。

| 编号 | 模块 | 场景 | 必测设备 |
|---|---|---|---|
| S1 | 资源准备 | 首次启动 SoundFont 下载 | 模拟器或真机 |
| S2 | 文件导入 | 导入 MIDI 到“仅伴奏”练习页 | 模拟器或真机 |
| S3 | 固定控制栏 | 播放、分小节导航、速度与 AB 循环 | 模拟器或真机 |
| S4 | 设置页 | 调整默认速度 | 模拟器或真机 |
| S5 | 交互 MusicXML 谱面 | 离线排版、小节跳转与播放同步 | 真机必测 |

## 5. 详细验收场景

### S1. 首次启动 SoundFont 下载

目的：确认首次运行时音色库准备流程可用，失败时用户知道如何恢复。

前置条件：清理应用数据，或使用首次安装状态启动 App。

步骤：

- [ ] 保持网络可用，启动 App 后观察 SoundFont 状态
- [ ] 首页或播放器页应展示检查中 / 下载中 / 已就绪等状态
- [ ] 下载完成后状态应变为音色已就绪
- [ ] 导入 MIDI 后应能使用 SoundFont 正常播放
- [ ] 断网或阻断下载后重新启动 App
- [ ] 下载失败时应出现失败提示和重试入口
- [ ] 点击重试后不会崩溃，状态能重新进入准备流程

通过标准：音色准备成功后可以播放 MIDI；失败路径有明确提示和重试入口。

### S2. 导入 MIDI

目的：确认用户能从文件系统导入标准 MIDI 文件，并进入当前首页可到达的 MIDI-only 练习路径。

前置条件：准备一份有效 MIDI 文件和一份异常 MIDI 文件。

步骤：

- [ ] 点击首页「导入乐谱文件」
- [ ] 选择一份有效 MIDI 文件
- [ ] 导入完成后自动进入 `ScorePracticePage`
- [ ] 页面标题应显示当前曲目名称，谱面区域显示“仅伴奏”及导入 MusicXML 提示
- [ ] 页面底部显示固定控制栏
- [ ] 返回首页后尝试导入异常 MIDI 文件
- [ ] 导入异常文件时应显示解析失败提示，而不是崩溃

通过标准：有效 MIDI 进入“仅伴奏”练习页且保留播放能力；无效文件有错误提示。

### S3. 固定控制栏

目的：确认练习页固定控制栏使用当前曲目的小节映射，并提供实际可用的播放控制。

前置条件：已完成 S2，`ScorePracticePage` 中已有有效 MIDI-only 曲目，SoundFont 已就绪。

步骤：

- [ ] 点击播放，伴奏开始；点击暂停，播放停止推进且没有持续长音
- [ ] 再次播放能从暂停位置继续
- [ ] 使用「上一小节」和「下一小节」跳转；MIDI-only 曲目按拍号推算的小节边界定位
- [ ] 打开「播放速度」Action Sheet，选择一个倍率后，控制栏标签和播放速度更新
- [ ] 在两个不同播放位置依次点击 AB：首次设置 A 点，第二次设置 B 点并启用循环，第三次清除 AB 循环

通过标准：播放、暂停、前后小节、速度和 AB 三段循环均由固定控制栏完成，不要求进度条、轨道列表或快进/后退控件。

### S4. 设置页调整默认速度

目的：确认设置页能保存默认速度，能显式应用到当前曲目，并影响后续导入曲目的初始速度。

前置条件：已导入一首 MIDI，并确认播放器可正常播放。

步骤：

- [ ] 从首页或练习页右上角进入「设置」
- [ ] 调整「默认速度」滑块
- [ ] 点击「应用到当前曲目」后，当前播放器速度应更新
- [ ] 返回练习页并播放，确认速度变化可感知
- [ ] 重新导入一首 MIDI，新曲目应使用设置中的默认速度
- [ ] 点击「恢复默认」后，默认速度回到推荐值

通过标准：设置值能被保存、应用，并影响新导入曲目的初始速度。

### S5. 可交互 MusicXML 谱面

目的：确认 MusicXML 在当前练习页离线排版，并与播放器保持同一小节映射。

- [ ] 断网后导入 MusicXML，谱面仍能显示
- [ ] 点击首、中、末小节，播放器跳到对应小节起点
- [ ] 播放中点击后继续播放，暂停中点击后保持暂停
- [ ] 连续播放跨小节时高亮只移动一次且必要时自动滚动
- [ ] 手动滚动后不会立刻被自动跟随拉回；再次播放操作后恢复跟随
- [ ] 双指缩放和纵向滚动不误触小节跳转
- [ ] 横竖屏切换后小节点击仍准确
- [ ] 后台切回后谱面与播放位置一致
- [ ] MIDI-only 曲目显示“仅伴奏”，播放功能仍可使用
- [ ] 复杂反复谱显示按谱面顺序播放提示

通过标准：MusicXML 练习页保持离线可用，小节交互与播放器使用同一小节映射；PDF 不作为练习页的直接展示格式。

## 6. 高级演奏台专项（不纳入本次发布阻断）

`PlayerPage` 仍保留 USB MIDI 发现、轨道选择和 `MidiFollowModeSession`，但当前首页不提供前往该页的产品入口。因此 USB MIDI 热插拔、钢琴轨静音恢复与长休止跟随不属于本次从产品入口可完成的发布阻断验收。

当提供调试入口或后续产品入口时，再以 iPhone 真机、class-compliant USB MIDI 电子琴和包含长休止的分轨 MIDI，验收以下专项：

- [ ] USB MIDI 设备发现、拔出与重连状态
- [ ] 跟随模式打开 / 关闭、钢琴轨静音及原始状态恢复
- [ ] 长休止期间等待与下一次有效起拍恢复

## 7. 发布判定

- [ ] 自动化质量门禁全绿
- [ ] S1-S5 当前产品入口可达的核心场景全部通过
- [ ] 若存在已知问题，已记录影响范围和规避方式
- [ ] iPhone 真机上至少完成一次交互 MusicXML 谱面验收
- [ ] 若本版本提供高级演奏台入口，已完成相应 USB MIDI 专项验收；未提供入口时已将该专项列为非阻断已知范围

满足以上条件后，可以进入打包或小范围试用发布。
