# 项目领域词汇

- **MIDI 轨道**：MIDI 文件中的原始 `MidiTrackInfo`，包含轨道名、通道、GM program 和音符时间线。
- **声部来源**：原始 MIDI 中的 `(trackIndex, channel set)` 切片；用于正确处理一个轨道包含多个乐器 channel 的 Format 0 文件。
- **可选声部**：声部选择界面中的一个选项；可对应一个声部来源，也可把多个钢琴来源合并为一个“钢琴”选项。
- **显示选择**：当前用于生成五线谱的一组可选声部 ID；它不改变播放的原始 MIDI。
- **全局默认规则**：跨曲目保存的乐器类别集合。没有曲目默认时，用它匹配当前曲目的可选声部。
- **曲目默认**：按稳定曲目指纹保存的具体声部 ID 集合，优先于全局默认规则。
- **显示会话**：复用原始 `MidiSongData`，但附带生成 MusicXML 和小节边界的 `ScoreSession`；切换显示会话不得重置播放状态。
- **总谱**：用户同时选择两个或更多可选声部后，由这些声部组成的一份多 part 五线谱。
